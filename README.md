# 🍽 Utro Bot v2

Telegram бот для автоматической публикации ежедневных постов о **кулинарных праздниках** с ПП/Кето-рецептами, сгенерированными искусственным интеллектом.

## ✨ Возможности

### 🆕 Новое в v2:
- 🥘 **Только кулинарные праздники** — 3 праздника еды в каждом посте (365 дней в базе)
- 📝 **Шаблоны текста** — Короткий (800 симв), Средний (1024), Длинный (4096) или Кастомный
- 🎨 **Выбор модели** — DALL-E 3 или Flux (Together AI)
- 📷 **Пост из фото** — Отправь фото блюда и получи пост!
- ✏️ **Редактирование** — Редактируй текст перед публикацией
- 🔗 **Ссылка на канал** — Автоматически добавляется в каждый пост
- ⚙️ **Настройки** — Полное управление через меню бота

### Основные функции:
- 📅 **Кулинарные праздники**: Локальная база + Calendarific API
- 🤖 **AI-контент**: Генерация уникальных текстов с помощью GPT-4o mini
- 🥗 **ПП/Кето-рецепты**: Здоровые рецепты с правильными подсластителями
  - Стевия: в КАПЛЯХ (3-5 капель = 1 ст.л. сахара)
  - Эритрит: 1:1 с сахаром
  - Аллюлоза: 0.7:1 к сахару
- 🎨 **AI-изображения**: DALL-E 3 или Flux на выбор
- ⏰ **Автопостинг**: Утренний пост с изображением в заданное время
- 🔧 **Админ-панель**: Управление ботом через Telegram

## 📋 Требования

- Python 3.10+
- Ubuntu 20.04+ (или другой Linux с systemd)
- VPS с минимум 512MB RAM
- API ключи (см. ниже)

## 🔑 Получение API ключей

### 1. Telegram Bot Token
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен вида `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2. OpenAI API Key
1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в [API Keys](https://platform.openai.com/api-keys)
3. Создайте новый ключ
4. Пополните баланс (минимум $5)

### 3. Calendarific API Key (бесплатно)
1. Зарегистрируйтесь на [calendarific.com](https://calendarific.com/)
2. Получите бесплатный API ключ (1000 запросов/месяц)
3. Этого достаточно для ежедневного использования

### 4. Together AI API Key (опционально, для Flux)
1. Зарегистрируйтесь на [api.together.xyz](https://api.together.xyz/)
2. Получите API ключ
3. Есть бесплатные кредиты для начала

### 5. Channel ID
1. Создайте канал в Telegram
2. Добавьте бота как администратора канала
3. Отправьте любое сообщение в канал
4. Используйте [@getidsbot](https://t.me/getidsbot) для получения ID
5. ID будет в формате `-100XXXXXXXXX`

### 6. Admin User ID
1. Напишите [@userinfobot](https://t.me/userinfobot)
2. Скопируйте свой User ID

## 🚀 Установка на VPS

### Шаг 1: Подключение к серверу

```bash
ssh root@your_server_ip
```

### Шаг 2: Обновление системы

```bash
apt update && apt upgrade -y
apt install -y python3 python3-pip python3-venv git
```

### Шаг 3: Клонирование репозитория

```bash
cd /root
git clone https://github.com/your-repo/utro.git
cd utro
```

### Шаг 4: Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

### Шаг 5: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 6: Настройка конфигурации

```bash
cp .env.example .env
nano .env  # или vim .env
```

Заполните все переменные:

```env
BOT_TOKEN=ваш_токен_бота
OPENAI_API_KEY=ваш_openai_ключ
HOLIDAYS_API_KEY=ваш_calendarific_ключ
CHANNEL_ID=-100ваш_id_канала
ADMIN_USER_IDS=ваш_user_id
TIMEZONE=Europe/Moscow
MORNING_POST_TIME=08:00
```

### Шаг 7: Защита конфигурации

```bash
chmod 600 .env
```

### Шаг 8: Тестовый запуск

```bash
python bot_v2.py
```

Проверьте:
- Бот отвечает на `/start`
- Кнопка "📅 Сегодня" генерирует превью поста
- Настройки работают через "⚙️ Настройки"

Остановите бот: `Ctrl+C`

## 🔧 Настройка systemd

### Шаг 1: Копирование сервиса

```bash
cp utro-bot.service /etc/systemd/system/
```

### Шаг 2: Перезагрузка systemd

```bash
systemctl daemon-reload
```

### Шаг 3: Включение автозапуска

```bash
systemctl enable utro-bot
```

### Шаг 4: Запуск сервиса

```bash
systemctl start utro-bot
```

### Шаг 5: Проверка статуса

```bash
systemctl status utro-bot
```

## 📊 Мониторинг

### Просмотр логов в реальном времени

```bash
journalctl -u utro-bot -f
```

### Просмотр последних 100 строк логов

```bash
journalctl -u utro-bot -n 100
```

### Просмотр логов за сегодня

```bash
journalctl -u utro-bot --since today
```

### Проверка статуса

```bash
systemctl status utro-bot
```

## 🛠 Управление ботом

### Перезапуск

```bash
systemctl restart utro-bot
```

### Остановка

```bash
systemctl stop utro-bot
```

### Запуск

```bash
systemctl start utro-bot
```

### Отключение автозапуска

```bash
systemctl disable utro-bot
```

## 📱 Команды бота

### Публичные команды
- `/start` — Приветствие и информация о боте
- `/help` — Справка по боту

### Админ-команды (только для ADMIN_USER_IDS)
- `/post_now` — Отправить пост прямо сейчас
- `/status` — Статус бота, аптайм, следующий пост
- `/test_holidays` — Проверить API праздников
- `/admin` — Справка по админ-командам

## 🐛 Устранение неполадок

### Бот не запускается

1. Проверьте логи:
```bash
journalctl -u utro-bot -n 50
```

2. Проверьте .env файл:
```bash
cat /root/utro/.env
```

3. Проверьте права:
```bash
ls -la /root/utro/.env
```

### Праздники не загружаются

1. Проверьте API ключ Calendarific
2. Используйте `/test_holidays` для диагностики
3. Бот автоматически использует fallback через GPT

### Изображения не генерируются

1. Проверьте баланс OpenAI
2. Проверьте API ключ
3. Бот отправит пост без изображения как fallback

### Пост не отправляется в канал

1. Убедитесь, что бот — администратор канала
2. Проверьте CHANNEL_ID (должен начинаться с `-100`)
3. Проверьте права бота на публикацию

### Высокое потребление памяти

1. Перезапустите бот:
```bash
systemctl restart utro-bot
```

2. Проверьте лимиты в сервисе (MemoryMax=512M)

## 💰 Примерные расходы

### OpenAI API
- GPT-4o mini: ~$0.15/1M input tokens, ~$0.60/1M output tokens
- DALL-E 3 (1024x1024): $0.04/изображение
- **Примерно**: $0.05-0.10 за пост
- **В месяц**: $1.50-3.00 при ежедневных постах

### Calendarific
- Бесплатно (1000 запросов/месяц)

### VPS
- Минимальный VPS: $3-5/месяц

## 📁 Структура проекта

```
utro/
├── bot.py                    # Главный файл
├── config.py                 # Конфигурация
├── keyboards.py              # Клавиатуры
├── handlers/
│   ├── __init__.py
│   ├── admin.py             # Админ-команды
│   └── common.py            # Публичные команды
├── services/
│   ├── __init__.py
│   ├── scheduler.py         # Планировщик
│   ├── holidays_api.py      # API праздников
│   ├── ai_content.py        # GPT генерация
│   ├── image_generator.py   # DALL-E генерация
│   └── post_service.py      # Постинг
├── data/
│   └── quotes.json          # Цитаты дня
├── .env                     # Секреты (не в git)
├── .env.example             # Шаблон конфигурации
├── .gitignore
├── requirements.txt
├── utro-bot.service         # Systemd сервис
└── README.md
```

## 🔄 Обновление бота

```bash
cd /root/utro
git pull
source venv/bin/activate
pip install -r requirements.txt
systemctl restart utro-bot
```

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Если возникли проблемы, создайте Issue в репозитории или свяжитесь с разработчиком.

---

**Сделано с ❤️ для любителей вкусной и полезной еды**
